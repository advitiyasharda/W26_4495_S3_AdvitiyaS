node_modules/
.next/
out/
build/
.env*.local
.DS_Store

# Runtime databases
data/doorface.db

# ML model artifacts (too large / binary — download separately)
models/*.task
models/*.pt
models/*.onnx
# Face biometric templates — local only (regenerate via register_faces.py)
models/face_encodings.npz
models/face_encodings_meta.json
# YOLOv8 training runs
runs/

# Virtual environment — never commit
venv/
.venv/

# URFD dataset videos — too large for git (300MB+)
data/urfd/

# Python cache
__pycache__/
*.pyc