/** KPI card insight modal + scroll targets for “Jump to charts below” */
export type InsightId = "traffic" | "alerts" | "falls" | "objects" | "camera";

export type DashboardScrollSection = "pipelines" | "access" | "security" | "vision" | "safety";
